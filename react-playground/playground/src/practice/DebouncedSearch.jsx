import React, {useState, useEffect} from "react";

//Implement a search input field that delays the API call until the user stops typing (debouncing).

const DebouncedSearch = () => {
    //set state for the query
    //set state for the debounced query

    //create useeffect for setting the timeout for the query 

    //return with the search bar and results in a <p>
}

export default DebouncedSearch